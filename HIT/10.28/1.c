#include <stdio.h>
#include <stdlib.h>
#include <string.h>
typedef struct node {
    char name[20];
    float rain, max, min, mean;
} ST;
enum week  {Mon,Tue,Wed,Thur,Fri,Sat,Sun};
int main() {
    ST a[11];
    printf();
    system("pause");
    return 0;
}